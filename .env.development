# Testing mode — the `vite` dev server uses the LOCAL backend.
# Vite loads this file ONLY in development (`vite`), never in `vite build`,
# so production builds keep using the deployed backend from `.env`.
VITE_API_URL=http://localhost:5000/api/v1
